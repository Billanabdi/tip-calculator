// Good Luck! You got this 💪🏾
// Write your code here.

let bill = 160;
let tip;

if (bill <= 50 && bill >= 300) {
  tip = bill * 0.15;
  console.log(
    `The bill is ${bill} and the tip is ${tip} the cost is ${bill + tip}`
  );
} else {
  bill > 300;
  tip = bill * 0.2;
  console.log(
    `The bill is ${bill} and the tip is ${tip} the cost is ${bill + tip}`
  );
}
